italia_save_mafalda = { ### 1/25
	icon = GFX_operations_save_mafalda
	map_icon = GFX_operations_coup_government_map
	name = italia_save_mafalda_name
	desc = italia_save_mafalda_desc
	days = 30

	allowed = {
		TAG = ITA
	}
	available = { 
		GER = { controls_state = 60 }
		network_strength = {
			target = GER
			state = 110
			value > 50
		}

	}
	
	visible = {
		has_global_flag = mafalda_arrested
		FROM = { tag = GER }
	}
	operatives = 2

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 50
	
	equipment = {
		infantry_equipment = 100
		support_equipment = 100
	}

	on_start = {  # SCOPE_OPERATION
		
	}
	outcome_potential = {
		custom_effect_tooltip = save_mafalda_outcome_tt
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.4
	experience = 50
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		random_list = {
			75 = { #Raid Succeeds
			ITA = {
				set_country_flag = mafalda_safe_and_sound
				country_event = { id = italia_ita.212 days = 2 }
			} 
			25 = { #Raid Fails
				}
			}
		}
	}
	
	# Custom Phases for Mafalda Rescue
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration 
		save_mafalda_infiltration = { base = 100 }
	}
	phases = { 
		save_mafalda_attack = { base = 100 }
	}
	phases = { 
		save_mafalda_exfiltration = { base = 100 }
	}
}

italia_assassinate_rosselli_bros = { ### 
	icon = GFX_operations_assassinate_rosselli
	map_icon = GFX_operations_coup_government_map
	name = italia_assassinate_rosselli_name
	desc = italia_assassinate_rosselli_desc
	days = 30

	allowed = {
		original_tag = ITA
	}
	available = { 
		has_government = fascism
	}
	
	visible = {
		has_completed_focus = ITA_assassinate_roselli_brothers
		NOT = { has_global_flag = rossellis_killed }
		FROM = { tag = FRA }
		has_government = fascism
		has_global_flag = rosselli_radio
		all_country_with_original_tag = {
			original_tag_to_check = SPR
			has_civil_war = no
		}
	}
	
	operatives = 1

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 20
	
	equipment = {
		infantry_equipment = 100
	}

	on_start = {  # SCOPE_OPERATION
		
	}
	outcome_potential = {
		custom_effect_tooltip = assassinate_rosselli_outcome_tt
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.4
	experience = 50
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		random_list = {
			95 = { #Raid Succeeds
			ITA = {
				country_event = { id = italia_ita.29 days = 2 }
			} 
			5 = { #Raid Fails, maybe disaster?
				}
			}
		}
	}
	
	# Custom Phases for Rosselis
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration 
		assassinate_rosselli_infiltration = { base = 100 }
	}
	phases = { 
		assassinate_rosselli_attack = { base = 100 }
	}
	phases = { 
		assassinate_rosselli_exfiltration = { base = 100 }
	}
}

lvn_kill_hitler = { ### 
	icon = GFX_operations_assassinate_hitler
	map_icon = GFX_targeted_sabotage_map
	name = lvn_kill_hitler_name
	desc = lvn_kill_hitler_desc
	days = 30

	allowed = {
		original_tag = ITA
	}
	available = { 
		has_country_leader = { name = "Pius XII" ruling_only = yes }
	}
	
	visible = {
		GER = { has_country_leader = { name = "Adolf Hitler" ruling_only = yes } }
		has_global_flag = LVN_canaris_plan_active
		FROM = { tag = GER }
	}
	
	operatives = 1

	# Integer [0-100] - Every operation has a hard requirement of network strength in the target country
	network_strength = 40
	
	equipment = {
		infantry_equipment = 100
	}

	on_start = {  # SCOPE_OPERATION
		
	}
	outcome_potential = {
		custom_effect_tooltip = lvn_kill_hitler_outcome_tt
	}

	outcome_modifiers = { operation_outcome }
	cost_modifiers = { operation_cost }
	risk_modifiers = { operation_risk }

	risk_chance = 0.4
	experience = 50
	# outcome is an effect that runs when the operation has completed
	outcome_execute = { # SCOPE_OPERATION
		random_list = {
			95 = { #Raid Succeeds
			GER = {
				country_event = { id = italia_ita.114 days = 2 }
			} 
			5 = { #Raid Fails, maybe disaster?
				}
			}
		}
	}
	
	# Custom Phases for Hitler Mission
	# Each phase for an operation may have a single actual phase assigned to it, which is determined here
	phases = { #infiltration 
		lvn_kill_hitler_infiltration = { base = 100 }
	}
	phases = { 
		lvn_kill_hitler_attack = { base = 100 }
	}
	phases = { 
		lvn_kill_hitler_exfiltration = { base = 100 }
	}
}
