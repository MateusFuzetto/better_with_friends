bur_peace = {
	
    bur_peace_with_sov = {
	
		icon = GFX_decision_oppression
	
        allowed = { tag = BUR }
		
        available = {
            NOT = { has_war_with = SOV }
        }
		
		activation = {
			has_war_with = SOV
        }
		
        is_good = yes
        selectable_mission = no
        days_mission_timeout = 30
		
        timeout_effect = {
            country_event = bur.1
        }
		
        complete_effect = {
			
        }
		
    }
	
}