LVN_armistice_category = {

	LVN_armistice_mission = {
		
		priority = 2000
		
		days_mission_timeout = 45
		
		available = {
			hidden_trigger = {
				has_country_flag = blocked 
			} #This will simply timeout
		}
		
		days_remove = 10
		
		is_good = no
		
		modifier = {
			political_power_factor = -0.5
		}
		
		visible = {
			original_tag = ITA
			NOT = {	has_country_flag = ITA_fascist_war }
			NOT = {	has_country_flag = ITA_communist_war }
		}
		
		activation = {# Gets triggered by event
			always = no
		}
		
		timeout_effect = { #Mussolini deposed, Italy switches sides
			ROOT = { country_event = { id = italia_ita.1 days = 3 } }
			custom_effect_tooltip = LVN_terms_will_be_worse_tt
			set_global_flag = italy_seeks_armistice 
		}
		
		cancel_trigger = {
			OR = {
				has_war = no
				custom_trigger_tooltip = {
					tooltip = ITA_controls_all_core_states
						all_of_scopes = {
							array = ITA.european_core_states
							is_fully_controlled_by = ROOT
						}
					hidden_trigger = {
						controls_state = 114
					}
				}
			}
			2 = {
				is_controlled_by = ITA
				days_since_last_strategic_bombing > 10
			}
		}
		
	}
	
}#Closing Bracket
	

